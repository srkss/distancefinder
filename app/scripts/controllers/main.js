'use strict';

/**
 * @ngdoc function
 * @name distancefinderApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the distancefinderApp
 */
angular.module('distancefinderApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
